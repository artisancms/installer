#! /usr/bin/env php
<?php

use ArtisanCms\NewCommand;
use ArtisanCms\InstallCommand;
use Symfony\Component\Console\Application;

require 'vendor/autoload.php';

$app = new Application('ArtisanCMS Studio', '0.1.9');

$app->add(new NewCommand);
$app->add(new InstallCommand);

$app->run();
